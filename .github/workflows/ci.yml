name: CI

on:
  pull_request:
    branches: [main, master, develop]
  push:
    branches: [main, master, develop]

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Enable corepack
        run: corepack enable

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"

      - name: Install FFmpeg + OpenCV (Video Worker real-binary tests)
        run: |
          sudo apt-get update && sudo apt-get install -y ffmpeg
          pip install opencv-python-headless numpy

      - name: One-export-per-file boundary check
        run: pnpm check:file-boundaries --all

      - name: Lint (includes max-lines, complexity, DDD boundaries)
        run: pnpm lint

      - name: Type check
        run: pnpm type-check

      - name: Test with coverage (90% gate)
        run: pnpm test:coverage

      - name: Build
        run: pnpm build
